server:
  port: 8081
spring:
  application:
    name: spgrtimesheetback
  datasource:
    url: jdbc:sqlserver://localhost;database=MDM-240401;trustServerCertificate=true
    username: dev
    password: s3cret
  jpa:
    hibernate:
      ddl-auto: validate
      naming.physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      
   
